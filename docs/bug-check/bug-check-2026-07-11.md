# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-11 00:40

## 检查范围
- **前端**（client 目录）：
  - lint：通过（0 errors）
  - test：通过（79 files，1180 tests）
  - build：通过（✓ built in 8.39s）
  - 代码质量审查范围：src/pages、src/components、src/hooks、src/utils
- **后端**（server 目录）：
  - lint：通过（0 errors，534 warnings 均为 P2 级别）
  - test：通过（73 files，1445 tests）
  - build：通过（tsc 编译成功）
  - 代码质量审查范围：src/routes、src/services、src/middleware、src/websocket
- **代码变更分析**：git diff HEAD~1，覆盖最近一次提交（refactor: 清理后端测试文件 any 类型注解）涉及的 11 个文件

## 已修复的问题
| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P1 | server/src/services/time-bank.service.ts | `updateOrderStatus` 函数中 accept/start/cancel 三个动作采用"先 SELECT 检查状态再 UPDATE"模式，但未使用 `transaction` 包裹且 SELECT 未加 `FOR UPDATE` 行锁。并发请求（如服务提供者快速双击"接受"按钮）可能同时通过状态检查，导致订单状态机一致性被破坏。对比同文件 `completeOrder` 已正确使用事务+行锁，此处属于遗漏。 | 将 SELECT + 状态校验 + UPDATE 包裹在 `transaction(async (client) => {...})` 中，SELECT 改为 `FOR UPDATE`，所有 `query()` 改为 `client.query()`，事务内重新查询更新后订单行返回。通知逻辑保持事务外 fire-and-forget。 | 6cc199e |
| 2 | P1 | server/src/services/kitchen-order.service.ts | `confirm` 函数采用"先 SELECT 检查状态再 UPDATE"模式，未使用 `transaction` 且 SELECT 未加 `FOR UPDATE`。并发确认请求可能同时通过 `status === 'pending'` 检查，破坏订单状态机一致性。对比同文件 `complete` 已正确使用事务+行锁，此处属于遗漏。 | 将 SELECT + 权限/状态校验 + UPDATE 包裹在 `transaction(async (client) => {...})` 中，SELECT 改为 `FOR UPDATE`，`query()` 改为 `client.query()`，事务内返回订单行。通知逻辑保持事务外。 | 6cc199e |

**同步修复的测试文件**（随同一提交）：
- `server/src/services/__tests__/time-bank.service.test.ts`：updateOrderStatus 的 11 个测试用例从 mock `query` 改为 mock `transaction` + `mockClient.query`，适配事务调用路径
- `server/src/services/__tests__/kitchen-order.service.test.ts`：confirm 的 4 个测试用例从 mock `mockQuery` 改为 mock `mockClientQuery`，适配事务调用路径

## 未修复的问题（需评估）
| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| 无 | - | - | - | - |

## 记录的轻微问题（P2，未修复）
- 后端 lint 有 534 个 warning（主要为 `@typescript-eslint/no-explicit-any` 和 `no-unused-vars`），分布在测试文件与服务文件中，不影响功能与构建
- `server/src/services/time-bank.service.ts` 的 family binding 系列方法（`createFamilyBinding`/`confirmFamilyBinding`/`rejectFamilyBinding`/`unbindFamilyBinding`）采用"先查后写"模式未使用事务，但不涉及资金/积分流转，影响范围有限，降为 P2
- `server/src/services/emergency.service.ts:670` 使用 `INTERVAL '${penaltyConfig.banInterval}'` 拼接 SQL，但 `penaltyConfig` 来自硬编码常量 `PENALTY_CONFIG`，非用户输入，无注入风险，仅风格层面可优化为参数化

## 验证结果
- 修复后 lint：**通过**（0 errors，534 warnings 均为 P2）
- 修复后 test：**通过**（73 files，1445 tests passed）
- 修复后 build：**通过**（tsc 编译成功，exit code 0）

## 深度审查结论
- **安全**：前端无 `dangerouslySetInnerHTML`（无 XSS）；后端动态 SQL 的表名/列名均为硬编码常量，值均参数化（无 SQL 注入）；图片 URL 有白名单校验
- **错误处理**：所有路由通过 `asyncHandler` 包装，未处理的 rejection 由全局 `errorHandler` 统一捕获
- **资源清理**：WebSocket 心跳定时器在 `close` 事件中正确 `clearInterval`；AI 调用超时定时器在 `finally` 中正确 `clearTimeout`；前端 `useEffect` 清理函数完备
- **类型安全**：`rows[0].xxx` 访问前均有 `rows.length` 守卫
- **资金安全**：`credit.service`、`skill-order.service`、`group-order.service`、`time-bank.transfer/donate/completeOrder`、`kitchen-order.complete/cancel` 均已正确使用事务+行锁
