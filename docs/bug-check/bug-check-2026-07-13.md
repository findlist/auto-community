# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-13 00:27

## 检查范围
- 前端（client）：lint / test / build 结果
- 后端（server）：lint / test / build 结果
- 代码变更分析范围：`git diff HEAD~1`（最近一次提交 `daa78ce` —— emergency.service SELECT * 替换为精确列名）

### 前端检查结果
| 项目 | 结果 | 说明 |
| --- | --- | --- |
| lint | 通过 | `eslint src --ext .ts,.tsx` 无报错 |
| test | 通过 | 79 个测试文件，1178 个测试全部通过（Duration 78.09s） |
| build | 通过 | `vite build` 成功，产物正常生成 |

### 后端检查结果
| 项目 | 结果 | 说明 |
| --- | --- | --- |
| lint | 修复前失败 / 修复后通过 | 原失败：`auth.test.ts:65` `NotFoundError` 未使用导入 |
| test | 通过 | 73 个测试文件，1439 个测试全部通过（Duration 9.44s） |
| build | 通过 | `tsc` 编译成功 |

### 代码变更分析（HEAD~1 → HEAD）
最近一次提交 `daa78ce refactor: emergency.service 5 处 SELECT * 替换为精确列名` 将 `emergency.service.ts` 中 5 处 `SELECT *` 替换为显式列名常量：
- `EMERGENCY_REQUEST_COLUMNS`（16 列，不含 `deleted_at`）
- `EMERGENCY_RESPONSE_COLUMNS`（11 列）
- `FALSE_REPORT_COLUMNS`（12 列）

已与数据库迁移文件核对：
- `001_init.sql`：emergency_requests（16 列 + deleted_at）、emergency_responses（9 列）、false_reports（11 列）
- `002_emergency.sql`：emergency_requests +type、emergency_responses +eta +timeout_at
- `009_emergency_enhancements.sql`：false_reports +resolution

列定义与表结构完全对齐，列名为硬编码常量非用户输入，无 SQL 注入风险。相关测试全部通过，重构安全。

## 已修复的问题
| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | server/src/routes/__tests__/auth.test.ts | 第 65 行导入了 `NotFoundError` 但全文未使用，触发 `@typescript-eslint/no-unused-vars` 规则，导致后端 `npm run lint` 失败，CI 会被阻塞 | 从 import 语句中移除 `NotFoundError`，仅保留实际使用的 `UnauthorizedError, BadRequestError` | 23a5379 |

## 未修复的问题（需评估）
| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
| --- | --- | --- | --- | --- |
| 无 | - | - | - | - |

## 记录的轻微问题（P2，未修复）
- 无

## 验证结果
- 修复后 lint：通过（server `eslint src` 无报错；client 一直通过）
- 修复后 test：通过（server auth.test.ts 19 个测试通过；全量 1439 通过未受影响）
- 修复后 build：通过（server `tsc` 成功；client 一直通过）
