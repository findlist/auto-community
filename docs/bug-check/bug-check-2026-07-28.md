# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-28 00:30

## 检查范围
- 前端：lint 通过 / test 部分失败（已存在问题，与本次修复无关）/ build 通过
- 后端：lint 通过 / test 全通过（81 文件 1874 用例）/ build 通过
- 代码变更分析范围：git diff HEAD~7（60afc5b → 4279f29）
- 前后端状态机一致性检查结果：发现 2 处状态机问题并修复
- 端到端关键路径可达性检查结果：核心路径均可达
- 前后端数据类型/字段一致性检查结果：发现 4 处字段不一致并修复

## 已修复的问题
| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P0 | server/src/routes/reports.ts | 创建举报接口字段名 targetType/targetId（camelCase）与前端 axios 拦截器转换后的 snake_case 不匹配，导致 validate 校验失败返回 422 | 将接口字段统一为 target_type/target_id，同步更新 validate 与 handler | 60afc5b |
| 2 | P0 | server/src/routes/emergency.ts | 举报虚假求助接口字段名 requestId 与前端转换后的 request_id 不匹配，导致接口不可用 | 将字段统一为 request_id，同步更新 validate、handler 与审计中间件 getResourceId | 60afc5b |
| 3 | P0 | server/src/routes/kitchen.ts | 创建订单接口字段名 postId/pickupType/pickupTime/deliveryAddress（camelCase）与前端转换后的 snake_case 不匹配 | 将字段统一为 snake_case，handler 中映射为 service 层期望的 camelCase | 60afc5b |
| 4 | P0 | client/src/api/timeBank.ts | TimeBank 订单状态更新前端发送 status 字段（名词），后端期望 action 字段（动词），且值的形态不同，导致状态更新失败 | 新增 STATUS_TO_ACTION 映射，将状态名词转换为 action 动词 | c405915 |
| 5 | P1 | server/src/services/skill-order.service.ts + server/src/routes/skills.ts | 技能订单状态机缺少 in_progress 分支：前端在 accepted 状态下显示"开始服务"按钮发送 status=in_progress，但后端 switch 无对应 case 导致状态不更新（状态死锁） | 新增 startOrder service 方法处理 accepted→in_progress 流转，路由 switch 增加 case 'in_progress' 分支 | a7d2db4 |
| 6 | P1 | client/src/pages/Emergency/index.tsx | canComplete 条件未检查 request.status === "responding"，导致求助已 resolved 后仍可能显示"完成"按钮但后端会拒绝 | 在 canComplete 条件中增加 request.status === "responding" 检查 | c405915 |
| 7 | P1 | client/src/pages/SkillExchange/Dispute.tsx | navigate 路径错误：导航到 /skill-exchange/orders（不存在），应为 /skills/orders（与 App.tsx 路由配置一致） | 将所有 navigate("/skill-exchange/orders") 修改为 navigate("/skills/orders") | c405915 |
| 8 | P1 | client/src/pages/TimeBank/__tests__/MyOrders.test.tsx | 测试断言未对齐 bug 修复：updateOrderStatus 增加了 actualDuration 第三参数，但测试断言仍用 2 参数 | 更新 5 处 toHaveBeenCalledWith 断言，补充第三参数（undefined 或 durationMinutes） | 4279f29 |
| 9 | P1 | client/src/pages/SkillExchange/__tests__/Dispute.test.tsx | 测试断言未对齐 bug 修复：navigate 路径从 /skill-exchange/orders 改为 /skills/orders，但测试断言仍用旧路径 | 更新 3 处 navigate 断言为 /skills/orders | 4279f29 |

## 未修复的问题（需评估）
| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 1 | P1 | client/src/pages/Admin/__tests__/ReportManagement.test.tsx | "列表加载成功显示举报数据"测试失败：`expect(screen.getByText('共 3 条'))` 找不到文本 | 经 git stash 验证为已存在问题（修改前即失败），与本次 bug 修复无关。疑似分页组件文本格式变化导致，需单独排查 |
| 2 | P1 | client/src/pages/Admin/__tests__/ContentReview.test.tsx | "列表加载成功显示内容数据"测试失败：类似分页文本匹配问题 | 经 git stash 验证为已存在问题，与本次 bug 修复无关 |
| 3 | P2 | server/src/routes/__tests__/*.test.ts（stash@{0}） | 存在另一会话的未提交测试文件修改（22 文件）在 stash 中 | 属于另一并行任务的工作，不在本次 bug 检查范围内 |

## 记录的轻微问题（P2，未修复）
- client/src/pages/Admin/ReportManagement.tsx 分页组件"共 N 条"文本格式与测试期望不一致（已存在）
- client/src/pages/Admin/ContentReview.tsx 同上分页文本问题（已存在）
- 前端 vitest 在全量运行时偶现 worker forks 超时（环境问题，非代码 bug）

## 状态机一致性检查明细
| 页面 | 按钮/操作 | 前端显示条件 | 后端前置条件 | 是否一致 |
|---|---|---|---|---|
| SkillExchange/Orders | 开始服务 | accepted + isSeller | order.status === 'accepted' && seller_id 匹配 | 一致（修复后） |
| SkillExchange/Orders | 完成服务 | in_progress + (isBuyer\|isSeller) | order.status === 'in_progress' | 一致 |
| TimeBank/MyOrders | 接受 | pending + isProvider | order.status === 'pending' && provider_id 匹配 | 一致（修复后） |
| TimeBank/MyOrders | 开始服务 | accepted + isProvider | action='start' && order.status === 'accepted' | 一致（修复后） |
| TimeBank/MyOrders | 完成服务 | in_progress + isRequester | action='complete' && order.status === 'in_progress' | 一致（修复后） |
| Emergency/index | 完成求助 | isRequester && hasArrivedResponse && request.status === "responding" | request.status === 'responding' && response.status === 'arrived' | 一致（修复后） |
| SkillExchange/Dispute | 返回订单列表 | 始终显示 | 无后端校验（纯前端导航） | 一致（修复后） |
| Reports | 创建举报 | 已登录 | authenticate + validate | 一致（修复后） |
| Kitchen/Orders | 创建订单 | 已登录 | authenticate + validate + orderLimiter | 一致（修复后） |
| Emergency | 举报虚假求助 | 已登录 | authenticate + createPostLimiter + validate | 一致（修复后） |

## 端到端关键路径检查明细
| 路径 | 起始状态 | 目标状态 | 是否可达 | 备注 |
|---|---|---|---|---|
| 用户注册 | 未登录 | 已注册 | 可达 | /register 路由正常 |
| 用户登录 | 已注册未登录 | 已登录 | 可达 | /login 路由正常 |
| 浏览技能列表 | 已登录 | 查看技能 | 可达 | /skills 路由正常 |
| 技能订单流转 | pending | accepted → in_progress → completed | 可达（修复后） | in_progress 分支已补全 |
| 厨房订单创建 | 已登录 | 订单已创建 | 可达（修复后） | 字段命名已统一 |
| 时间银行订单流转 | pending | accepted → in_progress → completed | 可达（修复后） | status→action 映射已修复 |
| 紧急求助完成 | responding | completed | 可达（修复后） | canComplete 条件已补全 |
| 争议处理 | disputed | resolved | 可达 | /skill-exchange/orders/:id/dispute 路由正常 |
| 争议页返回订单列表 | 争议页 | 订单列表 | 可达（修复后） | navigate 路径已修正 |
| 创建举报 | 已登录 | 举报已创建 | 可达（修复后） | 字段命名已统一 |
| 管理员审核 | 已登录管理员 | 内容已审核 | 可达 | /admin 路由正常 |
| 管理员用户管理 | 已登录管理员 | 用户已管理 | 可达 | /admin/users 路由正常 |

## 验证结果
- 修复后 lint（后端）：通过
- 修复后 lint（前端）：通过
- 修复后 test（后端）：通过（81 文件 1874 用例全部通过）
- 修复后 test（前端）：本次修复的测试文件全部通过（MyOrders 22 用例 + Dispute 18 用例）；已存在的 2 个 Admin 测试失败与本次修复无关
- 修复后 build（后端）：通过
- 修复后 build（前端）：通过
