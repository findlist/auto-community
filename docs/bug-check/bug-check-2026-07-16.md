# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-16 01:15

## 检查范围
- **前端（client 目录）**：
  - `npm run lint`：通过
  - `npm run test`：通过（1181 个测试）
  - `npm run build`：通过
  - 代码质量检查范围：`src/pages`、`src/components`、`src/hooks`、`src/utils`
- **后端（server 目录）**：
  - `npm run lint`：通过
  - `npm run test`：通过（1615 个测试）
  - `npm run build`：通过
  - 代码质量检查范围：`src/routes`、`src/services`、`src/middleware`、`src/utils`
- **代码变更分析范围**：`git diff HEAD~1` 及最近一批提交（6a88cd2 / 68a3f8d / 6b9f4d1）

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P0 | server/src/services/notification-channels.ts | `dispatchExternalChannels` 直接将数据库密文 `user.phone` 传入短信网关 payload，导致短信发送失败且可能将密文暴露给外部通道 | 调用 `decryptPhone` 解密后再传入 payload；解密失败时 warn 日志并跳过短信通道，不阻塞站内通知 | 68a3f8d |
| 2 | P1 | server/src/services/auth.service.ts | `forgotPassword` 中 `logger.info` 携带明文 `code` 字段，日志泄露后验证码可被冒用重置密码 | 删除日志入参中的 `code` 字段；用户存在/不存在两分支均不再记录明文验证码 | 6a88cd2 |
| 3 | P1 | server/src/middleware/auditLog.ts | `sanitizeRequestBody` 仅处理一层对象，嵌套结构（如 `{ users: [{ password }] }`）内的敏感字段未脱敏，PII 可经嵌套结构泄露到审计日志 | 递归处理嵌套对象与数组；引入 `MAX_SANITIZE_DEPTH=5` 防止恶意超深嵌套导致栈溢出 | 68a3f8d |
| 4 | P1 | server/src/utils/sanitize.ts | `validateImageUrl` 路径遍历防护仅校验 `..`，未覆盖 Windows 反斜杠 `\\` 与 URL 编码变体 `%2e`/`%5c`，可被绕过读取任意文件 | 增加 `\\`、`%2e`、`%5c` 及大小写变体校验 | 68a3f8d |
| 5 | P1 | server/src/services/emergency.service.ts | `respondToEmergency` 中 `INSERT emergency_responses` 与 `UPDATE emergency_requests` 两条 SQL 非事务包裹，前者成功后者失败会留下孤儿响应记录且紧急请求状态不一致 | 用 `transaction` 包裹两条 SQL，保证原子性 | 68a3f8d |
| 6 | P1 | server/src/services/group-order.service.ts | `create` 函数中三条 SQL（INSERT group_orders / INSERT participant / UPDATE count）非事务包裹，部分失败会留下 `current_participants=0` 的孤儿拼单，破坏后续 join/cancel 逻辑 | 用 `transaction` 包裹三条 SQL，保证原子性 | 68a3f8d |

### 配套测试补充

| 提交哈希 | 说明 |
|---|---|
| 68a3f8d | auditLog 新增 3 用例覆盖嵌套对象/数组内嵌对象/超深递归脱敏；notification-channels.test 增加 crypto mock；emergency.service.test 与 group-order.test 更新为 transaction 内 `mockClient.query` 调用 |
| 6a88cd2 | auth.service.test 新增 2 个安全不变式用例，遍历 `logger.info` 入参断言不含 `code` 字段 |
| 6b9f4d1 | sanitize.test 补全 5 用例覆盖反斜杠/URL编码 `%2e`/`%5c`/大写 `%2E` 变体拦截与合法路径不误判 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 1 | P1 | server/src/middleware/rateLimiter.ts | `memoryStore` 为内存存储的限流计数器，无定期清理机制，长期运行下过期 key 仍占用内存，存在缓慢内存泄漏 | 修复需新增定时清理任务或引入 LRU/TTL 淘汰策略，属于较大改动且可能影响现有限流语义，记录为"需评估"，不在本次最小修复范围内 |

## 记录的轻微问题（P2，未修复）

- 暂未发现新增 P2 问题。本次检查聚焦最近一批提交的安全与数据一致性，历史 P2 项见此前各日报告。

## 验证结果
- 修复后 lint：通过（client + server）
- 修复后 test：通过（client 1181 个测试，server 1615 个测试）
- 修复后 build：通过（client + server）

## 备注
- 本次检查与自动开发任务并行执行。本会话识别并修复的 6 个 P0/P1 问题已由并行任务在以下提交中完成提交，本会话仅补充生成检查报告，未产生新的代码变更：
  - `6a88cd2` fix: auth.service 日志中移除明文验证码
  - `68a3f8d` fix: auditLog 递归脱敏嵌套对象与数组 + notification-channels/emergency/group-order/sanitize 一并修复
  - `6b9f4d1` test: 补全 sanitize 路径遍历变体防护测试 5 用例
- 未修复项 #1（rateLimiter memoryStore）建议在下一次专项重构中评估，可考虑替换为 Redis 后端或增加定时清理。
