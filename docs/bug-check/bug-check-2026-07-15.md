# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-15 00:45

## 检查范围
- **前端**（client 目录）：
  - lint：通过（0 errors）
  - test：通过（79 test files, 1181 tests passed）
  - build：通过（首次失败为 AddressBook.tsx 瞬时文件写入不完整，重试后通过）
- **后端**（server 目录）：
  - lint：通过（0 errors）
  - test：通过（77 test files, 1560 tests passed）
  - build：通过
- **代码变更分析范围**：
  - 最近一次提交（HEAD）：`0485059 refactor: SharedKitchen/Orders 原生 confirm 替换为自定义 Modal`
  - 前序提交：`99b83f9 refactor: AddressBook 原生 confirm 替换为自定义 Modal`
  - 前序提交：`8b2ff54 fix: 应急页面展示异常 - 修复前后端字段名不匹配与种子数据状态错误`
  - 工作区未提交变更（并行样式优化任务）：12 个 client 文件
  - 子代理深度搜索：SQL 注入、XSS、未处理 Promise、资源泄漏、类型断言滥用

## 已修复的问题
| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P0 | client/src/api/upload.ts | 响应拦截器返回完整 `ApiResponse<T>`（`{ code, message, data }`），但 `uploadImage`/`uploadImages` 泛型第二参数误标为 `UploadResult`/`MultiUploadResult`，导致调用方 `res.url`、`result.images` 运行时为 `undefined`，图片上传功能完全不可用 | 改为 `client.post<never, ApiResponse<UploadResult>>` 并取 `res.data` 返回业务数据，与 auth.ts/address.ts 等其他 API 文件统一 | e6bd3d3 |
| 2 | P1 | client/nginx.conf | 后端 multer 配置单文件 5MB、批量最多 5 张（约 25MB），但 Nginx 默认 `client_max_body_size` 为 1MB，任何超过 1MB 的上传请求会直接返回 413 错误 | 补充 `client_max_body_size 30m` 配置，覆盖批量上传 + multipart 编码开销 | e6bd3d3 |

## 未修复的问题（需评估）
| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| 无 | — | — | — | — |

## 记录的轻微问题（P2，未修复）
- **server/src/services/skill.service.ts:215,219**：动态字段收集逻辑使用 `as SqlParam` 静默断言，与 `admin.service.ts:386` 等价逻辑使用 `isSqlParam()` 运行时守卫不一致。建议统一为 `isSqlParam` 守卫以拒绝函数/Symbol 等非法类型。
- **server/src/middleware/auth.ts:131,136**：JWT `expiresIn` 使用 `as unknown as` 双重断言绕过 `@types/jsonwebtoken` 字面量联合类型限制。库类型与运行时不匹配导致，注释已说明，运行时由 jsonwebtoken 库自身校验。建议在 `env.ts` 中将 `JWT_EXPIRES_IN` 收窄为模板字面量联合类型从源头消除。
- **工作区 P2 样式优化**（未提交，属并行样式优化任务）：Auth 页面 focus shadow、Home/SharedKitchen/SkillExchange/TimeBank 标题 `text-balance`、Dashboard hover scale、CSS `scroll-behavior: smooth` 与 `::selection` 配色等，均为视觉体验优化，非 bug。

## 验证结果
- 修复后 lint：通过
- 修复后 test：通过（ImageUpload 11 tests + HomepageImage 11 tests + 全量 1181 tests）
- 修复后 build：通过（client build 成功，server build 成功）

## 检查方法说明
1. **构建验证**：对 client 和 server 分别运行 `npm run lint`、`npm run test`、`npm run build`
2. **Diff 分析**：使用 `git diff HEAD~1`、`git diff HEAD~1..HEAD`、`git diff` 分析最近提交与工作区变更
3. **调用链追踪**：对 `upload.ts` 的类型变更，追踪了 `client.ts` 响应拦截器实现、`HomepageImage.tsx` 和 `ImageUpload.tsx` 调用方、以及全量 API 文件的泛型使用模式
4. **深度搜索**（子代理）：覆盖 SQL 注入（参数化查询 + 白名单）、XSS（`dangerouslySetInnerHTML`）、未处理 Promise（`safeNotify` 收口）、资源泄漏（`try/finally` + close 回调）、类型断言滥用（`as unknown as` / `as any`）
5. **配置合规**：验证 nginx.conf 包含 `port_in_redirect off`、`absolute_redirect off`、`server_name_in_redirect off`、`try_files $uri $uri/ $uri/index.html`、gzip 完整配置等项目硬约束
