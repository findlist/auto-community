# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-18 00:30

## 检查范围

### 前端（client 目录）
- `npm run lint`：通过（eslint 无报错）
- `npm run test`：通过（79 文件 / 1190 测试全部通过，duration 84.80s）
- `npm run build`：通过（vite 构建成功，duration 2m 23s）
- 代码质量检查范围：src/pages、src/components、src/hooks、src/utils、src/api、src/stores

### 后端（server 目录）
- `npm run lint`：通过（eslint 无报错）
- `npm run test`：通过（81 文件 / 1643 测试全部通过，duration 18.81s）
- `npm run build`：通过（tsc 编译成功）
- 代码质量检查范围：src/routes、src/services、src/middleware、src/config、src/utils、src/websocket、src/jobs

### 代码变更分析范围
- `git diff HEAD~1`：最近一次提交 `4e05c71 feat: time-bank family 四处路由接入 auditMiddleware 审计追踪`
- 变更内容：server/src/routes/time-bank.ts 中 4 个亲情绑定路由（POST /family、PUT /family/:id/confirm、PUT /family/:id/reject、PUT /family/:id/unbind）接入 auditMiddleware
- 变更评估：审计中间件接入方式正确，getResourceId 回调正确引用 req.params.id，未引入新问题

### 深度代码审查
- 使用 search 子代理对前后端核心代码进行 7 大类问题扫描：异常处理、资源泄漏、认证/授权、并发/竞态、输入校验、错误信息泄露、数值/边界
- 额外扫描：XSS（dangerouslySetInnerHTML / innerHTML / eval）、SQL 注入（模板字符串拼接）

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P0 | server/src/services/time-bank.service.ts:940 | createFamilyBinding 用明文手机号 `WHERE phone = $1` 查询 AES 加密的 phone 字段，永远匹配不到记录，导致亲情绑定功能 100% 失效 | 改用 `hashPhone(parentPhone)` 查询 `phone_hash` 字段，与 auth.service 注册/登录逻辑保持一致 | e90e67e |
| 2 | P0 | server/src/websocket/index.ts:160 | WebSocket 鉴权 `jwt.verify(data.token, env.JWT_SECRET)` 未显式锁定 algorithms，存在 alg 混淆攻击风险，攻击者可伪造 token 冒充任意用户接入 WebSocket | 补齐 `{ algorithms: ['HS256'] }` 第三参数，与 middleware/auth.ts 三个入口对齐 | e90e67e |
| 3 | P0 | client/src/pages/Profile/DeleteAccount.tsx:100-106 | 在组件渲染期间直接调用 clearAuth()（zustand 状态更新）、localStorage.removeItem、navigate，违反 React 渲染纯函数规则，会导致状态不一致与重复执行 | 将副作用移入 useEffect 监听 status?.status，保留渲染守卫返回 null | 1a69cf3 |
| 4 | P1 | server/src/services/kitchen-order.service.ts:125 | create 函数未校验 quantity 为正整数，传入 0 会创建 0 份数无意义订单，传入负数会导致 remaining_portions 异常增加（库存放大漏洞） | 在份数比较前补加 `Number.isInteger && 1-100` 范围校验 | e90e67e |
| 5 | P1 | client/src/pages/Messages/Chat.tsx:180-181 | handleSendMessage 调用 wsClient.send() 后无条件清空输入框，WebSocket 断连时 send 返回 false，消息直接丢失且无任何提示 | 检查 send 返回值，失败时 toast.error 提示并保留输入内容 | 1a69cf3 |
| 6 | P1 | client/src/components/Map/LocationPicker.tsx:106-113 | dragend 回调只有 try/finally 无 catch，regeo 逆地理失败时异常成为 unhandled promise rejection，用户无感知 | 补加 catch 块，失败时清空地址避免 unhandled rejection | 1a69cf3 |
| 7 | P1 | client/src/pages/Admin/HomepageImage.tsx:72 | 重复保存时旧 setTimeout 未清理，2.5s 内多次保存会导致旧定时器提前关闭新保存的"已保存"提示 | 设置新定时器前先 `if (successTimerRef.current) clearTimeout(...)` | 1a69cf3 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| 1 | P0 | client 多处（api/client.ts、stores/authStore.ts、Login.tsx、Register.tsx） | Token 双重存储在 localStorage（auth-storage + token 两个 key），XSS 可窃取；清理逻辑分散在多处易遗漏导致状态不一致 | 架构性改动，涉及多个文件统一改造为单一真相源，需评估改造方案 |
| 2 | P0 | client 多处（ProtectedRoute.tsx、AdminRoute.tsx） | 路由守卫仅检查前端 isAuthenticated 状态，不校验 token 过期；user.role 来自 localStorage 可被浏览器控制台篡改绕过 AdminRoute | 架构性改动，需引入 JWT exp 解析或 /auth/me 校验机制，需评估 |
| 3 | P1 | server/src/services/admin.service.ts:42,52,64 | 用户搜索 `phone LIKE $1` 对加密字段模糊查询永远匹配不到；SELECT phone 返回密文管理员无法辨认 | 需设计 phone_last4 列或改用 phone_hash 等值查询，涉及 schema 变更，需评估 |
| 4 | P1 | server 多个路由（time-bank.ts、kitchen.ts、skills.ts、admin.ts） | 多个 POST/PUT 路由缺少 validate([...]) 输入校验中间件，req.body 直接透传 service 层 | 批量补全 validate 工作量大且需逐个设计校验规则，service 层有兜底校验，需评估 |
| 5 | P1 | server/src/services/backup.service.ts:64-97 | executeCommand 子进程无超时，pg_dump 挂起会导致定时备份任务积压、Promise 永不 resolve | 需设计超时逻辑与子进程 kill 策略，需评估 |
| 6 | P1 | server/src/config/redis.ts:90-98 | clearCachePattern 使用 KEYS 命令，O(N) 阻塞，生产环境 key 数量大时可能使 Redis 卡顿数秒 | 改用 SCAN 迭代式扫描需重构，需评估 |
| 7 | P1 | server/src/services/data-deletion.service.ts:234-279 | 审核状态更新与匿名化跨事务，executeAnonymization 失败时状态停留在 approved 而非回滚 pending | 需合并事务或增加回滚逻辑，改动较大，需评估 |
| 8 | P1 | server/src/services/group-order.service.ts:305-331 | 拼单退出退款 amount=1 时 Math.floor(0.9)=0 全额归发起人，边界策略对参与者不公 | 需产品确认退款规则，需评估 |
| 9 | P1 | client/src/components/Upload/ImageUpload.tsx:84-127 | ObjectURL 未在组件卸载时释放（内存泄漏）；批量上传结果匹配假设 images 长度等于文件数，不匹配时预览永久 uploading | 需加 useEffect cleanup 且涉及上传回调逻辑重构，需评估 |
| 10 | P1 | client/src/components/Toast/index.tsx:73-79,112-116 | onClose 闭包每次渲染创建新引用，ToastItemView useEffect 反复重建 setTimeout，toast 显示时长可能超出 duration | 需用 useCallback 稳定 remove，涉及组件交互逻辑，需评估 |
| 11 | P1 | client/src/pages/Emergency/index.tsx:726-735 | handleReport 无防重复提交（可触发多次请求）；举报错误写入 setError 但 DetailView 在 request 已加载时不显示 error | 需加 reporting 状态且涉及错误展示逻辑调整，需评估 |
| 12 | P1 | client/src/api/client.ts:46-69 | axios 网络错误/超时 error.response 为 undefined 时 `status ?? 500` 误报为 500，掩盖真实原因 | 需区分 ERR_NETWORK/ECONNABORTED 错误码，涉及多处错误处理逻辑，需评估 |
| 13 | P1 | client/src/pages/TimeBank/TransferModal.tsx:50, DonateModal.tsx:50 | validate() 在渲染期执行，用户开始输入时立即显示错误提示，UX 不友好 | 需改为提交时校验或增加 touched 标记，需评估 |

## 记录的轻微问题（P2，未修复）

- server/src/migrations/1704067200017_system_config_enhance.ts:25 — SQL 拼接硬编码预设值（key/value/desc），虽不构成注入风险（非用户输入）但风格不佳，建议改用参数化
- server/src/middleware/auditLog.ts:29-56 — sanitizeRequestBody 有 MAX_SANITIZE_DEPTH=5 限制且字段名精确匹配，嵌套超 5 层或字段名变体（如 user_phone、idCardNumber）可能漏脱敏
- server/src/utils/safeNotify.ts — Redis 不可用时 fire-and-forget 仅 warn 日志，关键通知（tokenBlacklist、notifyOrderStatusChange）失败可能被淹没，建议增加监控指标
- client/src/pages/Emergency/index.tsx:482-496 — navigator.geolocation.getCurrentPosition 回调未用 cancelled 标志，组件卸载后仍可能 setState（React 18 不再警告但仍是泄漏）
- client/src/components/Layout/index.tsx:215-221 — getUnreadCount Promise 未用 cancelled 标志且无定时轮询，长时间停留页面未读数不更新
- client/src/pages/Admin/HomepageImage.tsx 及其他地图组件 — 高德 SDK 在无 key 时仍拼接 key= 发起请求，建议复用 ResourceMap.tsx 的 hasKey 守卫
- server/src/services/kitchen.service.ts:261-301 — 更新帖子动态字段拼接可读性可优化，但逻辑正确

## 验证结果

### 修复后验证（提交后重新运行）

| 验证项 | 结果 |
|--------|------|
| 后端 lint（eslint） | 通过 |
| 后端 test（vitest） | 通过（81 文件 / 1647 测试，比修复前多 4 个测试用例自动通过） |
| 后端 build（tsc） | 通过 |
| 前端 lint（eslint） | 通过 |
| 前端 test（vitest） | 通过（79 文件 / 1190 测试） |
| 前端 build（vite） | 通过 |

### 提交记录

| 提交哈希 | 提交信息 | 涉及文件 |
|----------|----------|----------|
| e90e67e | fix: 修复亲情绑定查询加密手机号失效、WebSocket JWT 未锁定算法、订单份数未校验三个问题 | server/src/services/time-bank.service.ts、server/src/websocket/index.ts、server/src/services/kitchen-order.service.ts |
| 1a69cf3 | fix: 修复注销页渲染期副作用、聊天消息丢失、地图逆地理异常、首页图片保存定时器泄漏四个问题 | client/src/pages/Profile/DeleteAccount.tsx、client/src/pages/Messages/Chat.tsx、client/src/components/Map/LocationPicker.tsx、client/src/pages/Admin/HomepageImage.tsx |

两个提交均已 push 至 origin/main（fe0d1f4..1a69cf3）。

## 审查覆盖范围说明

### 后端已深度审查
- services/：time-bank、user、admin、auth、credit、ai、emergency、kitchen、kitchen-order、skill-order、group-order、notification、reputation、backup、data-deletion（15 个核心服务）
- routes/：auth、users、admin、time-bank、kitchen、skills、emergency、upload（8 个核心路由）
- middleware/：auth、errorHandler、auditLog、rateLimiter、upload、validator（6 个）
- config/：database、redis、env（3 个）
- utils/：tokenBlacklist、idempotency、safeNotify、crypto（4 个）
- websocket/index.ts、jobs/scheduler.ts、index.ts

### 前端已深度审查
- pages/：Auth（4）、Admin（13）、Emergency（2）、Messages（1）、Profile（4）、SharedKitchen（7）、SkillExchange（5）、TimeBank（9）、Home（1）、NotFound（1）
- components/：Layout、Toast、Upload、Map、ErrorBoundary、ProtectedRoute、AdminRoute 等
- hooks/：useAuth、useFormValidation、useMediaQuery、useScrollReveal
- utils/：websocket、format、error、formValidation、ab-test、mockInterceptor
- api/：client、auth、caseConverter
- stores/：authStore

### 本次报告未覆盖（建议后续补充）
- server/src/services/ 下的 review.service.ts、message.service.ts、cache.service.ts、metrics-*、map.service.ts、storage-adapter.ts、ab-test.service.ts、address.service.ts、audit.service.ts、emergency-resource.service.ts、skill.service.ts
- server/src/routes/ 下的 messages.ts、notifications.ts、reports.ts、address.ts、ai.ts、ab-test.ts、metrics.ts、public.ts、health.ts
- 这些文件多为辅助功能或纯查询类，预计 P0/P1 风险低于已审查的核心业务文件
