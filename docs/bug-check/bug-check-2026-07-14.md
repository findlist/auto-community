# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-14 01:05

## 检查范围
- 前端（client）：lint / test / build 结果
- 后端（server）：lint / test / build 结果
- 代码变更分析范围：`git diff HEAD~1`（最近一次提交 `8c06dfb docs: 周评估修正过时进度声明与优化开发计划`，仅文档变更）；进一步回溯近 2 天代码提交，对 routes / services / middleware / utils / pages / components / hooks 做全量 P0/P1 静态审查

### 前端检查结果
| 项目 | 结果 | 说明 |
| --- | --- | --- |
| lint | 通过 | `eslint src --ext .ts,.tsx` 无报错 |
| test | 通过 | 79 个测试文件，1178 个测试全部通过（Duration 42.79s） |
| build | 通过 | `vite build` 成功，产物正常生成 |

### 后端检查结果
| 项目 | 结果 | 说明 |
| --- | --- | --- |
| lint | 通过 | `eslint src` 无报错 |
| test | 通过 | 77 个测试文件，1547 个测试全部通过（Duration 10.38s） |
| build | 通过 | `tsc` 编译成功 |

### 代码变更分析
最近一次提交 `8c06dfb` 为纯文档变更，无代码改动。本次检查对前后端核心目录做全量静态审查，重点关注：
- SQL 注入 / 越权访问 / PII 泄露（安全）
- 异常处理缺失 / 资源泄漏 / 外部 API 超时（健壮性）
- 类型断言 / 逻辑边界 / 状态管理（正确性）

已确认安全的检查项：所有 `${}` 模板插值均为受控常量（硬编码列名 / 白名单字面量）；`authenticate` + `requireRole` 鉴权链完整；`upload.ts` 用 `memoryStorage` + MIME 白名单 + `crypto.randomBytes` 防路径穿越；`sanitize.ts` 用 xss 库清洗富文本；`transaction` 在 `finally` 中释放 `PoolClient`；`ai.service.ts` 的 `callLLM` / `generateEmbedding` 已有 `AbortController` 超时；`scheduler.ts` 每个任务独立 try/catch；`gracefulShutdown` 按序关闭各类资源；`dangerouslySetInnerHTML` 全项目无使用；`addEventListener` 均正确配对 `removeEventListener`；WebSocket 在 cleanup 中调用 `close()`。

## 已修复的问题
| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
| --- | --- | --- | --- | --- | --- |
| 1 | P0 | server/src/services/user.service.ts:250 | `getVerificationStatus` 的 LEFT JOIN 条件 `u.id = vr.user_id AND vr.status != 'rejected' OR vr.status = 'rejected' AND u.verify_status = 'rejected'` 缺括号，SQL 中 AND 优先级高于 OR，导致第二个 OR 分支 `(vr.status = 'rejected' AND u.verify_status = 'rejected')` 完全没有 `u.id = vr.user_id` 用户隔离约束。任何 `verify_status='rejected'` 的用户在 LEFT JOIN 时会匹配到全局所有 rejected 认证申请记录，经 `ORDER BY vr.created_at DESC LIMIT 1` 后返回全局最新一条他人的认证申请，泄露 `real_name` 等敏感 PII | 为 OR 表达式补齐括号：`ON u.id = vr.user_id AND (vr.status != 'rejected' OR (vr.status = 'rejected' AND u.verify_status = 'rejected'))`，显式分组保证用户隔离 | d6c931e |
| 2 | P1 | server/src/utils/idempotency.ts:33 | `checkIdempotency` 中 `JSON.parse(value)` 无 try/catch。Redis 值若被污染/迁移损坏为非 JSON 字符串，会抛 SyntaxError 冒泡到所有依赖幂等的订单创建接口（skill-order / kitchen-order / emergency 等），导致 500 且用户无法下单。对比 `redis.ts:61`、`websocket/index.ts`、`ai.service.ts` 的 JSON.parse 均已包裹 try/catch，此处遗漏 | 在 JSON.parse 外补 try/catch，解析失败时 `logger.warn` 记录并返回 `{ hit: false }` 视为未命中，让业务重新执行而非阻塞主流程 | d6c931e |
| 3 | P1 | server/src/services/map.service.ts:54,100 | `geocode` / `regeo` 两处 `fetch(url.toString())` 无超时控制。高德 API 响应慢或网络挂起时 fetch 无限期等待，导致请求线程被占用、用户长时间无响应、高并发下连接池耗尽。对比 `ai.service.ts` 已用 `AbortController + setTimeout` 实现超时 | 两处均补 `AbortController` + `setTimeout(5000)` 5 秒超时，在 finally 中 `clearTimeout`，abort 触发的 AbortError 由外层 try/catch 捕获返回 null（符合现有降级逻辑） | d6c931e |
| 4 | P0 | client/src/pages/Emergency/index.tsx:384-405 | 高德地图 SDK 加载 useEffect 无 cleanup 函数。`window.AMap` 尚未加载完成时重复切换 `showMap` 会创建多个 script 标签堆积在 `<head>` 中。对比已修复的 `ResourceMap.tsx`（使用 `useAMapScript` hook）和 `LocationPicker.tsx`（完整 cleanup），此处属同类问题遗漏 | 移除冗余 `loadAMap` 内层函数，直接创建 script 并在 useEffect 返回 cleanup：`if (script.parentNode) script.parentNode.removeChild(script)`，移除未加载完成的 script 避免 DOM 堆积 | 64db7f2 |
| 5 | P1 | client/src/pages/Emergency/index.tsx:212-227,562-570,636-647,370-380 | 4 处 async 函数（`fetchResources` / `handleSubmit` / `handleConfirmArrival` / `handleRespond`）使用 `try { ... } finally { ... }` 无 catch 块。API 调用失败时异常向上冒泡为未处理 Promise rejection，用户无任何 UI 反馈（仅 submitting 状态被 finally 重置）。与已修复的"6 个页面 catch 块补充 toast.error"属同类问题，但 Emergency 这 4 处遗漏 | 4 处均补 catch 块，调用 `toast.error(getErrorMessage(err, "xxx失败"))` 给出可读提示；新增 `toast` / `getErrorMessage` 导入 | 64db7f2 |
| 6 | P1 | client/src/pages/SharedKitchen/GroupOrders.tsx:76-80 | `handleCreate` 成功后表单重置仅清空 `title` / `description` / `targetAmount` / `address` 4 个字段，遗漏 `minParticipants`（默认 3）/ `maxParticipants`（默认 10）/ `deadline`（默认 ""）。下次打开创建弹窗会保留上次的截止时间和参与人数，用户可能未察觉而提交错误数据（如已过期的 deadline） | 补充 `setDeadline("")` / `setMinParticipants(3)` / `setMaxParticipants(10)` 三行重置，恢复初始默认值 | 64db7f2 |

## 未修复的问题（需评估）
| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
| --- | --- | --- | --- | --- |
| 1 | P0 | client/src/pages/Messages/Chat.tsx:107 | WebSocket 连接通过 URL query 参数传递 JWT token（`?token=${token}`）。token 会出现在 Nginx access log、浏览器 Network 面板，存在敏感信息泄露风险 | 修复需前后端配合改造（改为连接后发送认证消息或使用 Sec-WebSocket-Protocol 子协议头），属于协议层重构而非最小必要代码改动，超出"只改导致 bug 的那几行"的修复边界，记录为需评估 |
| 2 | P1 | server/src/services/notification-channels.ts:80,238,248 | 邮件 SMTP `transporter.sendMail` / 阿里云 `sendSms` / 腾讯云 `SendSms` 三处外部调用无超时控制。SMTP 服务器响应慢或短信网关故障时 promise 长时间挂起，积累 pending Promise 占用内存 | 调用方 `dispatchExternalChannels` 已用 `Promise.allSettled` + `.catch(() => {})` 兜底，主业务流程不阻塞；为每个 SDK 包装 `Promise.race` 超时需改动 3 处且需测试 SMTP/短信 SDK 的超时行为，属于"需评估"范畴 |
| 3 | P1 | server/src/services/data-deletion.service.ts:272-279 | `executeAnonymization`（独立事务 T1）与后续 `UPDATE deletion_requests SET status='completed'`（独立 query Q2）非原子。T1 成功但 Q2 失败时，用户数据已匿名化（不可逆）但申请状态仍为 `approved`，管理员可能重复触发 | 修复需将状态更新合并到 `executeAnonymization` 同一事务内并修改函数签名传入 `requestId`，涉及调用方改动与事务边界调整，非最小必要代码改动，记录为需评估 |
| 4 | P1 | server/src/services/emergency.service.ts:573-584 | 完成求助奖励积分时手动实现 `UPDATE balance + INSERT transaction`，绕过 `creditService.earnCredits`。其他模块（kitchen-order / skill-order / time-bank / group-order）均统一走 creditService，设计不对称。若 creditService 未来增加风控 hook，emergency 模块会绕过 | 替换为 `creditService.earnCredits` 需核对签名与参数对齐（参考 group-order.service.ts:551 调用方式），且需验证事务上下文传递，属于跨模块重构，记录为需评估 |

## 记录的轻微问题（P2，未修复）
- server/src/utils/tokenBlacklist.ts:34-42 — Redis 异常时 `isBlacklisted` 降级返回 `false`（放行），这是与 `rateLimiter.ts` 一致的可用性优先降级权衡，建议补充 `logger.warn` 便于安全审计（当前静默降级）
- server/src/utils/crypto.ts:46 — `scryptSync` 同步阻塞事件循环（N=16384 时约 50-100ms），由于 `cachedKey` 缓存仅首次调用执行，影响较小；建议在应用启动时预派生密钥
- client/src/pages/Emergency/index.tsx:425-428 — `if (lng && lat)` 坐标判断在经纬度为 0 或 NaN 时行为异常（经纬度为 0 的边缘场景实际业务不会出现，NaN 时静默跳过无日志）
- client/src/pages/Profile/Verify.tsx:34-40, DeleteAccount.tsx:38-44 — `loadStatus` 未纳入 useEffect 依赖数组，违反 exhaustive-deps 规则，当前逻辑不会立即引发 bug
- client/src/pages/Home/index.tsx:123-138 — `/public/stats` 失败时仅 `console.error`，`totalUsers` 保持 null 导致 UI 无法区分"加载中"与"加载失败"
- client/src/pages/SharedKitchen/index.tsx, SkillExchange/index.tsx — useEffect 中 `if (loading) return` 闭包竞态隐患，建议改用 `cancelled` 标志模式与项目其他异步保持一致
- client/src/components/Layout/index.tsx:215-221 — `getUnreadCount` 失败仅 `console.error` 无降级提示，当前静默降级为 0 对次要功能可接受，建议降级为 `console.warn`

## 验证结果
- 修复后 lint：通过（client `eslint src --ext .ts,.tsx` 无报错；server `eslint src` 无报错）
- 修复后 test：通过（client 79 文件 1178 测试全部通过；server 77 文件 1547 测试全部通过，其中 idempotency.test.ts 新增"非法 JSON 视为未命中"用例验证修复行为）
- 修复后 build：通过（client `vite build` 成功；server `tsc` 编译成功）

## 提交记录
- `d6c931e` fix: user.service SQL JOIN 优先级修复 + map.service fetch 超时防御 + idempotency JSON 解析容错（后端，已推送 origin/main）
- `64db7f2` fix: Emergency 求助异常提示补全 + 高德地图 script 卸载清理 + GroupOrders 表单重置补全（前端，已推送 origin/main）
