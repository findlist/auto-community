# Bug 检查与修复报告 - auto-community

## 检查时间
2026-07-17 00:45

## 检查范围
- **前端**（client 目录）：
  - lint：通过
  - test：通过（79 个测试文件，1181 个测试用例）
  - build：通过
  - 代码质量审查范围：src/pages、src/components、src/hooks、src/utils
- **后端**（server 目录）：
  - lint：通过
  - test：通过（81 个测试文件，1636 个测试用例）
  - build：通过
  - 代码质量审查范围：src/routes、src/services、src/middleware、src/jobs
- **代码变更分析**：git diff HEAD~1（最近一次提交为 img alt 无障碍优化重构，无引入 bug）

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P1 | server/src/jobs/scheduler.ts | handleSkillOrderTimeout 竞态条件：SELECT 在事务外执行，UPDATE 仅按 id 过滤未带 status='pending'，期间订单若被接单则误取消已接单订单并错误退还积分 | UPDATE 增加 `AND status='pending'` 过滤与 `RETURNING id`，用 cancelledIds 集合过滤后续解冻循环 | eba33ad |
| 2 | P1 | server/src/jobs/scheduler.ts | handleKitchenOrderTimeout pending 路径同型竞态条件：SELECT 事务外执行，UPDATE 未过滤 status，期间订单若被确认则误取消已确认订单并错误恢复库存与退款 | 同上修复模式，UPDATE 增加 status 过滤与 RETURNING，库存恢复与退款循环均增加 cancelledIds 守卫 | eba33ad |
| 3 | P1 | server/src/routes/time-bank.ts | PUT /orders/:id/status（action=complete）缺少 actual_duration 正整数校验与 rating 1-5 范围校验，负数时长会破坏时间账本一致性，非法评分会污染信誉分计算 | 路由层补全 actual_duration 正整数校验与 rating 1-5 整数范围校验 | eba33ad |
| 4 | P1 | server/src/routes/skills.ts | PUT /orders/:id/status（status=completed）缺少 rating 1-5 范围校验与 review 长度校验，与 kitchen 路由不一致，非法评分会污染信誉分 | validate 数组增加 `body('rating').optional().isInt({min:1,max:5})` 与 `body('review').optional().isLength({max:500})` | eba33ad |
| 5 | P1 | client/src/pages/SharedKitchen/Detail.tsx | 详情页 useEffect 异步请求无 cancelled 标志，快速切换路由时旧请求返回会覆盖新数据，导致显示内容与路由 id 不一致 | useEffect 增加 cancelled 标志，所有 setState 前检查 cancelled，cleanup 函数置 cancelled=true | 3557310 |
| 6 | P1 | client/src/pages/SkillExchange/Detail.tsx | 同上竞态条件 | 同上修复模式 | 3557310 |
| 7 | P1 | client/src/pages/TimeBank/ServiceDetail.tsx | 同上竞态条件 | 同上修复模式 | 3557310 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 1 | P1 | client/src/pages/Emergency/index.tsx (DetailView) | fetchRequest 为 useCallback 包装在 useEffect 中触发，异步请求无 cancelled 标志，快速切换 requestId 时旧请求可能覆盖新数据 | fetchRequest 逻辑复杂（含定位、权限判断等），cancelled 标志无法直接包裹 useCallback 内部 setState，需重构 fetchRequest 签名，改动范围较大可能引入新问题 |
| 2 | P1 | client/src/pages/SkillExchange/index.tsx | 列表页 loadPosts 为 useCallback 包装，useEffect 调用时无竞态防护，快速切换 Tab/分类/搜索词时旧请求覆盖新数据 | loadPosts 内部多处 setState，cancelled 标志需传入函数参数或重构为 useEffect 内联，涉及函数签名变更与依赖数组调整，10 个列表页同型问题统一修复风险较高 |
| 3 | P1 | client/src/pages/SharedKitchen/index.tsx | 同上列表页 load 函数竞态问题 | 同上原因 |
| 4 | P1 | client/src/pages/TimeBank/index.tsx | 同上列表页 Promise 链竞态问题 | 同上原因 |
| 5 | P1 | client/src/pages/Admin/OrderManagement.tsx | 管理后台列表页 loadOrders 竞态，快速切换筛选条件时旧请求覆盖新数据 | 同上原因，且涉及 selectedIds 与错位数据配合的批量误操作风险 |
| 6 | P1 | client/src/pages/Admin/AuditLog.tsx | 同上管理后台列表页竞态 | 同上原因 |
| 7 | P1 | client/src/pages/Admin/UserManagement.tsx | 同上管理后台列表页竞态 | 同上原因 |
| 8 | P1 | client/src/pages/Admin/ContentReview.tsx | 同上管理后台列表页竞态，selectedIds 配合错位数据可能批量误操作 | 同上原因 |
| 9 | P1 | client/src/pages/Admin/ReportManagement.tsx | 同上管理后台列表页竞态 | 同上原因 |
| 10 | P1 | client/src/pages/Admin/Metrics.tsx | loadTrend 在切换 metric 时无 cancelled 标志，旧请求覆盖新数据 | 同上原因 |
| 11 | P1 | client/src/pages/Notifications/index.tsx | 加载更多 loadNotifications 并发时旧请求覆盖新数据 | 同上原因 |

## 记录的轻微问题（P2，未修复）

- **XSS 理论风险**：reviews.content 与 messages.content 在 5 处 service 写入点（review.service.ts、skill-order.service.ts、time-bank.service.ts、kitchen-order.service.ts、message.service.ts）未调用 sanitizeXss 清洗。经前端渲染核查，全部使用 React 安全文本插值 `{content}`，未使用 dangerouslySetInnerHTML/innerHTML，当前无真实 XSS 风险。建议作为防御纵深后续补全。
- **IDOR**：GET /users/:id 未校验所属权，任意已登录用户可查看其他用户基本资料，但手机号字段已脱敏，影响有限。
- **输入校验**：time-bank.ts 多个写操作路由（POST /services、PUT /services/:id、POST /orders、POST /family、POST /reviews、POST /disputes）未使用 validate 中间件，依赖 service 层手工校验兜底。skills.ts 与 kitchen.ts 的 PUT /posts/:id 同样缺失。当前 service 层有白名单防注入与部分范围校验，但缺乏统一入口校验。
- **可扩展性**：scheduler.ts handleDeferredTimeEarn 查询所有 pending 时间收益流水未加 LIMIT，若积压大量 pending 流水可能导致单事务内存占用过高与长事务。
- **代码风格**：ai 路由 /classify 接口手动校验未走统一 validate 中间件（逻辑正确，仅风格不统一）。

## 验证结果
- 修复后前端 lint：通过
- 修复后前端 test：通过（1181 个测试）
- 修复后前端 build：通过
- 修复后后端 lint：通过
- 修复后后端 test：通过（1636 个测试）
- 修复后后端 build：通过
